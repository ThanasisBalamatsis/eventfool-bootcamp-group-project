@model EventFool.Domain.Models.Ticket

@{
    ViewBag.Title = "Edit Ticket";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section style{
    <link rel="stylesheet" href="@Url.Content("~/Content/Background.css")" />
}
<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Ticket</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.QRCode)
        @Html.HiddenFor(model => model.Price)
        @Html.HiddenFor(model => model.PayPalReference)
        @Html.HiddenFor(model => model.EventId)
        @Html.HiddenFor(model => model.UserId)

        @*<div class="form-group">
                @Html.LabelFor(model => model.Active, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Active, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Active, "", new { @class = "text-danger" })
                </div>
            </div>*@

        <div class="form-group">
            @Html.LabelFor(m => m.Active, new { @class = "col-md-2 control-label" })

            <div class="col-md-10">
                Active
                @Html.RadioButtonFor(m => m.Active, true, new { @class = "mx-auto" })
                Inactive
                @Html.RadioButtonFor(m => m.Active, false, new { @class = "mx-auto" })

            </div>
        </div>


        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "IndexTicket")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
